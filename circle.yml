machine:
  ruby:
    version: 2.3.1
  services:
    - docker

test:
  override:
    - bundle
    - bundle exec rspec spec/docker_*_spec.rb:
        files:
          - spec/docker_*_spec.rb
        environment:
          DOCKER_CONTAINER: handson

