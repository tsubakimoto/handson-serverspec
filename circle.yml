machine:
  ruby:
    version: 2.3.1
  services:
    - docker

dependencies:
  override:
    - docker build -t handson ./docker/

test:
  override:
    - bundle
    - bundle exec rspec spec/docker_*_spec.rb
